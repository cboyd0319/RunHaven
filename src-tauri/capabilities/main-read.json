{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-read",
  "description": "Read-only access for RunHaven setup, dashboard, profile, and planning commands.",
  "windows": ["main"],
  "permissions": [
    "allow-get-setup-status",
    "allow-list-agents",
    "allow-get-dashboard-status",
    "allow-plan-run"
  ]
}
