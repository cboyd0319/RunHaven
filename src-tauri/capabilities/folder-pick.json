{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "folder-pick",
  "description": "Native directory picker for selecting a project folder.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
